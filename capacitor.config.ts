import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'livro-capitulos',
  webDir: 'dist/livro-capitulos/browser'
};

export default config;
